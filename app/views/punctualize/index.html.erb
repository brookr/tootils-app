<h1>Most Punctual!</h1>

<table>
  <tr>
    <th>User</th>
    <th>Average<br />Punctuality</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td>@<%=h user.name %></td>
    <td><%=h user.scores.first.average %></td>
  </tr>
<% end %>
</table>

<br />
